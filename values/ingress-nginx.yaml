controller:
  replicaCount: 1
  config:
    use-forwarded-headers: "true"
  service:
    annotations:
      nginx.ingress.kubernetes.io/enable-http2: "true"
  resources:
    requests:
      memory: 150Mi
    limits:
      memory: 200Mi
  metrics:
    enabled: true
