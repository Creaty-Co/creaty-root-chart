server:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: vault.creaty.club
  dataStorage:
    size: 5Gi
    storageClass: do-block-storage-xfs-retain
  logLevel: trace
  logFormat: standard

ui:
  enabled: true
