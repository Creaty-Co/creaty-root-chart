configs:
  cm:
    resource.exclusions: |
      - apiGroups:
          - cilium.io
        kinds:
          - CiliumIdentity
  params:
    server.insecure: "true"

server:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - argocd.creaty.club
